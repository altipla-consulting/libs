
project: libs.altipla.consulting

tools:
  database:
    container: mysql
    ports:
    - "3306:3306"

  phpmyadmin:
    container: phpmyadmin
    ports:
    - "5000:80"

  redis:
    container: redis
    ports:
    - "6379:6379"
